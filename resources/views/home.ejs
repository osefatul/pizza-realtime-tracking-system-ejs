<section class="hero sm:h-full">
    <div class="vanillaContainer mx-auto flex items-center justify-between">
        <div class="w-1/2">
            <h6 class="text-sm sm:text-lg"><em>Are you hungry?</em></h6>
            <h1 class="text-xl sm:text-3xl md:text-6xl font-bold">Don't wait !</h1>
            <button 
            class="text-[12px] sm:text-lg btn-primary px-2 sm:px-6 py-2 rounded-full text-white font-bold mt-6 ">
                Order Now!
            </button>
        </div>

        <div class="w-1/2">
            <img src="/img/hero-pizza.png" alt="">
        </div>
    </div>
</section>


<section class="menu vanillaContainer mx-auto py-10 sm:py-8 ">
    <h1 class="text-xl font-bold mb-8">All Pizzas</h1>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-x-12 gap-y-16">
        <% pizzas.map((pizza) => { %>
        <div class="w-full md:w-64">
            <img class="h-28 md:h-40 mb-4 mx-auto" src="/img/<%= pizza.image %>" alt="">
            <div class="text-center">
                <h2 class="mb-4 text-lg"><%= pizza.name %></h2>
                <span class="size py-1 px-4 rounded-full uppercase text-xs"><%= pizza.size %></span>
                <div class="flex items-center justify-center space-x-4 sm:space-x-0 sm:justify-around mt-6">
                    <span class="font-bold text-lg">
                        $<%= pizza.price %>
                    </span>
                    <button 
                    data-pizza="<%= JSON.stringify(pizza) %>"
                    class="sm:py-1  px-2 sm:px-6 rounded-full font-bold flex items-center justify-center sm:justify-between add-to-cart">
                        <span>+</span>
                        <span class="ml-1 sm:ml-3">Add</span>
                    </button>
                </div>
            </div>
        </div>
        <% }) %>

    </div>
</section>